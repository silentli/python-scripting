# archive-dir

Archive directories into timestamped ZIP files.

## Usage

```bash
python archive-dir.py --source-dir <path> --output-dir <path>
```

**Example:**
```bash
python archive-dir.py --source-dir ./my-project --output-dir ./backups
# Creates: ./backups/my-project_2024-01-15.zip
```
